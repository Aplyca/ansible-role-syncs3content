---
syncs3content:
  action: "{{ action }}"
  from:
    storage:
      bucket: app-storage
      dir: /mnt/storage/app
      profile: default
    database:
      dir: data/sql/mysql
      name: app
      host: localhost
      password: ""
      user: ""
      replacements: []
    post_commands: []
  to:
    storage:
      bucket: app-storage
      dir: /mnt/storage/app
      profile: default
    database:
      dir: data/sql/mysql
      name: app
      host: localhost
      password: ""
      user: ""
  slack:
    username: "Ansible"
    icon_emoji: slack
    channel: general
    message: "Sync content of *{{ app }}*"
    description: "The latest content of {{ app }} is in S3"

app: App
action: False

